## 🚀 Features

- Merge multiple PDF files into one
- Simple web interface
- Node.js backend
- Easy to run locally

---

## 🛠 Prerequisites

- Node.js
- npm

Check if installed:
```bash
node -v
npm -v


If not installed (Ubuntu):

sudo apt update
sudo apt install nodejs npm -y

Start the Node.js server:

node server.js

open index.html or pdfmergesnakem.html for pdf merging
